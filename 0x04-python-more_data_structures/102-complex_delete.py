#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    for keys in a_dictionary:
        if a_dictionary[key] == value:
            del key
        else:
            return a_dictionary
    return a_dictionary
